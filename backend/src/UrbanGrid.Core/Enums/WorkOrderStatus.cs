namespace UrbanGrid.Core.Enums;

public enum WorkOrderStatus  { PENDING, ASSIGNED, IN_PROGRESS, COMPLETED, CANCELLED }
public enum WorkOrderPriority { LOW, MEDIUM, HIGH, CRITICAL }
public enum WorkOrderSourceType { FAULT, INSPECTION, PLANNED }
